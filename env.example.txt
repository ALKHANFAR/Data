# Environment Variables Example
# Copy this content to .env file in production

# Security
SECRET_KEY=your-secret-key-here-use-strong-random-string
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"

# Database
DATABASE_URL=sqlite:///./data_cleaning.db
# For PostgreSQL: postgresql://user:password@localhost/dbname
# For Render PostgreSQL: will be provided by Render

# CORS (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,https://yourdomain.com

# Redis (optional - for Celery tasks)
REDIS_URL=redis://localhost:6379/0

# Debug Mode (set to False in production)
DEBUG=False

# Render-specific (automatically set by Render)
# PORT=10000
# PYTHON_VERSION=3.12.0


